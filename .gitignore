# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Ritual source footage — the Flow/Veo master clips (~7 MB each).
# The extracted frames in public/frames/ritual/ ARE committed; these masters are
# the input that produced them and are reproducible from WORKFLOW.md §Phase 0
# plus the ffmpeg pipeline recorded in DESIGN.md §6.5/§8.
public/*.mp4
public/*.mov

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
